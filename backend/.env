PORT=5000
MONOGDB_URL=mongodb://127.0.0.1:27017/twiller
FRONTEND_URL=http://localhost:3000

# Security and Gateway credentials (fill in if using real accounts)
STRIPE_SECRET_KEY=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Developer test settings (set to true to test payment and mobile login time windows outside IST constraints)
BYPASS_TIME_CHECK=true
